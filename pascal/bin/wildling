#!/bin/sh
DIR="$(cd "$(dirname "$0")" && pwd)"
BIN="$DIR/../dist/wildling"
if [ ! -x "$BIN" ]; then
    echo "wildling binary not found. Run pascal/build.sh first." >&2
    exit 1
fi
exec "$BIN" "$@"
